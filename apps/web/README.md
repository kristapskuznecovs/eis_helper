# Web App

Vite + React application for the project UI.

## Conventions

- keep route-level pages under `src/pages/`
- keep reusable UI in `src/components/`
- keep hooks in `src/hooks/` and shared types in `src/types/`
- keep mock/demo data isolated from UI composition
